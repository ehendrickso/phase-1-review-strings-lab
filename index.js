const currentUser = "User"
const excitedUser = currentUser.toUpperCase()

const firstLetterOfUser = currentUser.charAt(0)

const welcomeMessage = "Welcome to Flatbook, " + currentUser + "!"

const excitedWelcomeMessage = "WELCOME TO FLATBOOK, " + excitedUser + "!"

const shortGreeting = "Welcome, "+ firstLetterOfUser + "!"